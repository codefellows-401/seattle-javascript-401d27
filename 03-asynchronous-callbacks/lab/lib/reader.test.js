const reader = require('./reader.test.js');

// TODO: Define Test Suite
describe('When properly invoked...', () => {
  
  // TODO: Define Test Case 
  it('should open the three files successfully', () => {
    const input = '';
    const output = '';
    expect(input).toBe(output);
  });

});